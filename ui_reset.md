室内监控中心界面重构方案
现有界面分析

平台仓库当前的 indoor_center.html 文件使用暗色主题和六宫格布局展示各个监测插件。顶部有区域选择与刷新按钮，主内容区域按网格排列了电子围栏、动物入侵、温度监测、设备状态、消防与环境等六个模块，每个模块以卡片形式显示标题、状态、监测画面和一组控制按钮。底部状态栏显示系统和传感器状态。布局简洁，但存在以下不足：

静态六宫格：每个插件的卡片位置和功能固定，无法按照场景空间关系直观展示。只有二维画面，缺乏对设备位置、环境关系的态势感知。

缺乏融合证据链：各插件独立显示检测结果，运维人员需要在不同模块之间切换，难以了解同一事件在多模态下的证据及综合判断。

功能硬编码：卡片的按钮和控制逻辑由前端固定定义，新增插件时必须修改前端代码才能增加相应功能。

相比之下，室外监测中心已引入场景化导航、融合证据链和配置驱动 UI。outdoor_center.html 使用 --panel‑bg 等 CSS 变量统一主题颜色，在页面顶部/底部布局工具栏和状态栏；侧边栏模块条目采用可激活状态的样式；主内容区提供视频画面与检测统计的布局；底部状态栏显示系统状态；脚本中通过 switchModule、loadModuleData 等函数动态切换模块和加载数据。这种设计增强了可扩展性和态势感知。

重构总体目标

按照室外监控中心的重构思路，将室内监控界面升级为3D/2.5D 场景化导航+融合证据链+配置驱动的综合平台。目标包括：

3D/2.5D 场景化导航：利用 Three.js 或 Cesium 渲染站区室内模型，在场景中根据设备/摄像头位置显示图标。用户通过鼠标拖拽、缩放在场景中导航，点击设备图标可弹出摄像头实时画面、近期检测状态和健康指标，替换原有静态六宫格列表，从空间维度增强态势感知。Three.js 官方文档指出，该库可在网页中渲染包含灯光、材质和纹理的 3D 对象，从而创建丰富的三维场景。

多模态融合证据链展示：在实时视频画面下方增加“融合证据链”组件，分条列出视觉、声学、气体、热像等插件的检测结果和置信度，并按照融合策略显示综合判定与建议。来自 EmergentMind 的综述指出，多模态融合通过整合来自不同传感器的异构信息可提高推理的鲁棒性和准确性；显示各模态的证据及最终决策有助于运维人员理解每次报警背后的理由。组件应支持以条形图或时间轴形式交互，方便回溯事件。

配置驱动的动态插件面板：前端通过 /api/detect/{plugin_id}/capabilities 接口动态获取插件能力（支持的检测模式、可调参数、支持的操作等），根据返回的配置渲染控制面板。这样新增插件时无需修改前端，只需后端实现该接口和对应的插件逻辑。此设计可将室外中心的动态控制面板机制迁移到室内中心。

统一主题与模块化结构：保持与室外中心一致的暗色主题和 accent 颜色，合理划分导航区、主监控区和控制区。采用响应式布局，兼顾桌面和移动端。所有交互逻辑封装在独立的 JavaScript 模块中，易于维护和拓展。

具体实现方案
1. 构建 3D/2.5D 室内场景导航

模型准备与坐标标定：基于机房 BIM 模型或 CAD 平面图构建室内 3D 模型，可使用 Blender 导出为 GLTF/GLB 格式。为每台摄像头、传感器和关键设备预先定义室内坐标（例如 [0–1] 归一化或米制坐标），与三维模型坐标系对齐。

渲染与交互：在新页面中引入 Three.js 或 Cesium。使用 Scene、Camera、Renderer 创建基本渲染环境，加载室内模型并添加光源。Three.js 文档指出，Scene 用于容纳 3D 对象、光源和相机，Camera 定义可视范围和视角。在渲染循环中监听鼠标事件，实现拖拽旋转、滚轮缩放以及点击拾取。拾取命中设备图标时，通过弹窗或侧边栏展示设备的实时画面、最近检测结果和健康指标。

图标与状态更新：在场景中使用 Sprite 或 Mesh 制作设备图标，颜色或动画反映设备状态（正常、注意、告警）。前端定时通过 WebSocket 或轮询向后端请求各设备的状态和报警信息，更新图标颜色并在弹窗中显示最新检测结果。

2. 多模态融合证据链组件

后端数据结构：在融合引擎中，针对同一时刻或同一事件，将视觉、声学、气体、热像等插件的单模态结果及置信度打包为一条证据链，并附带融合后的综合结论和建议。返回 JSON 格式，例如：

{
  "timestamp": "2026-01-23T12:05:30Z",
  "modalities": [
    {"type": "vision", "result": "正常", "confidence": 0.92},
    {"type": "acoustic", "result": "异常", "confidence": 0.76},
    {"type": "gas", "result": "正常", "confidence": 0.95},
    {"type": "thermal", "result": "正常", "confidence": 0.88}
  ],
  "fusion_result": "注意", 
  "recommendation": "检查继保室 A 柜上方声源"
}


前端展示：在主视频画面下方新增证据链区域，使用表格或条形图展示每个模态的检测结果和置信度，通过颜色直观体现风险等级；综合结论置于显眼位置。提供时间轴交互，用户可回溯历史证据链记录并查看相应画面。可复用室外中心中用于展示多模态结果的设计，使用可折叠条目节省空间。

交互与分析：点击某一模态条目，可展开查看该模态的详细分析（例如热像图温度分布、声谱分析等）；点击综合结论可以查看融合算法解释。多模态融合原则指出，通过融合不同传感器的互补信息可以弥补单一模态的噪声和不确定性，提高系统鲁棒性。

3. 配置驱动的动态插件面板

扩展后端接口：为每个室内插件实现 /api/detect/{plugin_id}/capabilities 接口，返回插件支持的操作和可调参数。例如电子围栏插件可以返回可配置的围栏区域、越线阈值、联动报警动作；温度监测插件返回可选温度阈值、传感器列表等。接口格式可借鉴室外中心中的实现，统一字段命名，如 controls 数组包含每个控制项的 type（slider/select/button）、label、min/max/options 等。

前端动态渲染：在控制面板区域，根据当前选中的设备或插件，调用上述接口获取配置并生成对应的表单组件。使用 Vue/React 等框架可方便实现动态表单；若继续使用原生 JS，则在回调中遍历 controls 数组动态创建 HTML 元素并绑定事件处理器。按钮事件触发 /api/detect/{plugin_id}/command 接口执行相应操作。

热插拔支持：新增插件时，前端只需在场景图标中注册点击事件和插件 ID，其他 UI 控制面板由后端驱动生成，无需修改前端代码。这一点与室外中心通过脚本中的 switchModule 和 loadModuleData 动态加载模块数据的思路一致。

4. 统一主题和目录结构

CSS 主题：沿用室外监测中心的暗色主题和蓝色/绿色/橙色/红色 accent 颜色，通过在 :root 定义 CSS 变量统一控制面板背景、边框和高亮色。确保图标、按钮、状态徽标等组件风格一致。

页面布局：调整布局为三栏：左侧 3D/2.5D 场景导航；中间为实时视频画面和融合证据链；右侧为配置驱动的控制面板和告警日志列表。顶部工具栏包含区域选择、刷新、告警入口和全局启动按钮；底部状态栏显示系统状态、在线设备数量和更新时间。

模块划分：将每个监测插件的前端逻辑封装为独立模块，与后端插件 ID 对应。在三维场景中点击图标时载入相应模块脚本，并在控制面板区域渲染其配置界面。保持代码结构与室外中心相同，方便共享组件和样式。

5. 实施路线

需求分析与数据准备：整理机房三维模型、设备坐标、各插件的能力定义和 API 返回格式；确认需要展示的多模态信息种类与融合策略。

三维场景开发： 学习并引入 Three.js/Cesium，完成模型加载、场景渲染、基础交互；实现设备图标标定与点击事件；开发弹窗或侧边栏用于显示实时监控和状态。

证据链与控制面板实现：设计证据链数据结构与视觉风格；开发交互组件；实现 /api/detect/{plugin_id}/capabilities 接口并根据返回动态渲染控制面板；实现历史时间轴和回放功能。

后端接口与插件联动：对现有室内插件（电子围栏、动物入侵、温度/消防/环境监测）扩展能力接口；确保每个插件输出融合所需的标准化结果和置信度；实现融合算法和证据链服务；对接 PTZ 控制和告警系统。

测试与优化：在模拟和真实环境中测试 3D 导航性能、证据链准确性及 UI 交互流畅度；优化模型加载、内存占用和响应速度；根据运维人员反馈调整界面布局与交互细节。

上线与迭代：部署重构后的室内监控中心，进行用户培训和收集意见；持续根据现场需求增加新的插件和功能，通过配置驱动模式快速集成。
