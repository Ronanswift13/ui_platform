# led_indicator

Purpose: LED indicator samples for red, green, yellow, and off states.

Current pilot asset:
- `meter_led_indicator_green_001.fixture.json` is a deterministic mock image spec used only to verify replay plumbing and output metadata.

Required annotation before real acceptance:
- visible color state
- expected numeric value and unit
- lighting/ambient condition
- source provenance
