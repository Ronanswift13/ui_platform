# analog_boundary

Purpose: analog meter samples near angle/range boundaries while still readable.

Required annotation before real acceptance:
- meter_type
- boundary condition
- expected value and tolerance
- pointer_angle_deg
- allowed fallback_level range
- source provenance
