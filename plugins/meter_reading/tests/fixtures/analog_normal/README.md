# analog_normal

Purpose: clear front-view analog meter samples that should produce `reading_status=success` and `review_status=clear`.

Required annotation before real acceptance:
- meter_type
- expected value and unit
- pointer_angle_deg
- range_min / range_max
- source provenance
- reviewer and review date
