# analog_quality_fail

Purpose: analog meter samples that should not fabricate a reading because of blur, darkness, occlusion, or too-low resolution.

Required annotation before real acceptance:
- meter_type
- quality failure reason
- expected review_status
- expected failure_reason candidates
- source provenance
