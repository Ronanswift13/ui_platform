# Root Cause Prompt（busbar_inspection）

用于质量门禁失败、原因码错误、bbox 越界、告警错级等问题的根因分析。

## Template
- Symptom:
- Reproduction:
- First failing boundary (`plugin` / `detector` / `config` / `postprocess`):
- True root cause:
- Why tests missed it:
- Smallest safe fix:
- Regression test to add:
- Preventive action:
