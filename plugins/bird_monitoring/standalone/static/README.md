# standalone/static

Static assets for the standalone UI live here.

This directory is intentionally separate from the production detector. It may contain UI images, CSS, or JavaScript, but it must not contain model weights, replay fixtures, or simulation truth data.
